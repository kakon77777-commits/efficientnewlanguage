# Phase 7d: raise a custom exception, caught by type + bound via `as`.
def validate(n):
    if n < 0:
        raise ValueError("n must be non-negative")
    return n

try:
    validate(0 - 5) => r
    r^0
except ValueError as e:
    e^0
